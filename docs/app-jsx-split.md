/**
 * Découpe App.jsx — vague 1
 *
 * Déjà extrait :
 * - `src/lib/plan-account.js` — merge / persist plans
 * - `src/lib/plan-stats.js` — computeStats, badges
 * - `src/app-shell/` — AppShell, BottomNav, Loading
 * - (antérieur) AuthScreen, Dashboard, PlanTab, CoachCard, …
 *
 * Prochaines vagues (une à la fois) :
 * - SettingsDrawer + AppTopBar (+ notifications helpers)
 * - ProfileTab
 * - OnboardingWizard / steps
 * - generatePlan / moteur display helpers
 * - StravaSection / MonAllureCard
 */
